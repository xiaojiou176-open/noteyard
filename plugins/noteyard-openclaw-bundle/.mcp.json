{
  "mcpServers": {
    "notes-recover": {
      "command": "./bin/notes-recover-mcp",
      "env": {
        "NOTES_RECOVER_REPO_ROOT": "${NOTES_RECOVER_REPO_ROOT}",
        "NOTES_RECOVER_CASE_DIR": "${NOTES_RECOVER_CASE_DIR}",
        "NOTES_RECOVER_PYTHON": "${NOTES_RECOVER_PYTHON}"
      }
    }
  }
}
